// Hook deshabilitado temporalmente para evitar errores de React Hooks
// La sincronización se hace manualmente en PreferencesModal

export function usePreferencesSync() {
  // Hook vacío para evitar errores
} 